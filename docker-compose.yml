services:
  mcp-chat:
    build: .
    container_name: mcp-chat
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
